<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PalindromeChecker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="fontFamily">@font/poppins</item>
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.ActionBar.PalindromeChecker" parent="Theme.Material3.DayNight">
        <item name="fontFamily">@font/poppins</item>
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PalindromeChecker" parent="Base.Theme.PalindromeChecker" />
</resources>